<Window x:Class="AiTokenBot.Views.ModelEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Width="420" Height="350">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="DialogTitle" Text="添加模型" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"
                   Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="模型名称" FontSize="13" Margin="0,0,0,4"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
            <TextBox x:Name="TbName" Height="36"
                     Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                     Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                     BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

            <TextBlock Text="Model ID" FontSize="13" Margin="0,16,0,4"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
            <TextBox x:Name="TbModelId" Height="36"
                     Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                     Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                     BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="取消" Width="90" Height="38" Click="OnCancel"/>
            <Button Content="保存" Width="90" Height="38" Margin="12,0,0,0" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
