<UserControl x:Class="AiTokenBot.Views.LlmPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AiTokenBot.Views"
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="860">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="大模型" FontSize="24" FontWeight="SemiBold"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
            <TextBlock Text="管理 LLM 平台与模型" FontSize="13" Margin="0,4,0,0"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                       Opacity="0.6"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Platform List -->
            <Border Grid.Column="0" CornerRadius="8"
                    Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                    BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="16,14,16,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="平台列表" FontSize="12" Opacity="0.6" VerticalAlignment="Center"
                                   Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                        <Button Grid.Column="1" Content="+ 新增" Width="68" Height="28" FontSize="12"
                                Click="OnAddPlatform"/>
                    </Grid>

                    <ListBox x:Name="PlatformList" Grid.Row="1"
                             BorderThickness="0"
                             SelectionChanged="OnPlatformSelected">
                        <ListBox.Resources>
                            <Style TargetType="ListBox">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                            </Style>
                        </ListBox.Resources>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Border" Background="{TemplateBinding Background}"
                                                    BorderThickness="3,0,0,0" BorderBrush="Transparent"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background"
                                                            Value="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"/>
                                                    <Setter TargetName="Border" Property="BorderBrush"
                                                            Value="{DynamicResource {x:Static adonisUi:Brushes.AccentBrush}}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background"
                                                            Value="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                    <TextBlock Text="{Binding BaseUrl}" FontSize="11" Opacity="0.4" TextTrimming="CharacterEllipsis"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Right: Detail -->
            <Grid Grid.Column="2" x:Name="DetailPanel" Visibility="Visible">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Platform Config -->
                        <Border Padding="20" CornerRadius="8"
                                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                                BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"
                                BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="平台配置" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                    <Button Grid.Column="1" Content="编辑" Width="60" Height="28" FontSize="12"
                                            Click="OnEditPlatform"/>
                                    <Button Grid.Column="2" Content="删除" Width="60" Height="28" FontSize="12" Margin="8,0,0,0"
                                            Click="OnDeletePlatform"/>
                                </Grid>

                                <Grid Margin="0,16,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="名称" VerticalAlignment="Center" Opacity="0.6"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TbPlatformName" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Base URL" VerticalAlignment="Center" Margin="0,10,0,0" Opacity="0.6"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TbPlatformUrl" Margin="0,10,0,0" FontFamily="Consolas"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="API Key" VerticalAlignment="Center" Margin="0,10,0,0" Opacity="0.6"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TbPlatformKey" Margin="0,10,0,0"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                                               Text="••••••••"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Models -->
                        <Border Margin="0,16,0,0" Padding="20" CornerRadius="8"
                                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                                BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"
                                BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="模型列表" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                    <Button Grid.Column="1" Content="+ 添加模型" Width="90" Height="28" FontSize="12"
                                            Click="OnAddModel"/>
                                </Grid>

                                <ItemsControl x:Name="ModelList" Margin="0,12,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="12,10" Margin="0,0,0,4" CornerRadius="4"
                                                    Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="140"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding ModelId}" FontSize="13" Opacity="0.5"
                                                               Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
                                                    <Button Grid.Column="2" Content="编辑" Width="50" Height="24" FontSize="11"
                                                            Click="OnEditModel" Tag="{Binding}"/>
                                                    <Button Grid.Column="3" Content="删除" Width="50" Height="24" FontSize="11" Margin="4,0,0,0"
                                                            Click="OnDeleteModel" Tag="{Binding}"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
