<Window x:Class="AiTokenBot.Views.TransferDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="420" Height="280">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="DialogTitle" Text="转入资金" FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
            <TextBlock x:Name="DialogSub" Text="从主钱包转入 USDC 到机器人独立账户" FontSize="12" Opacity="0.5" Margin="0,4,0,0"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="金额 (USDC)" FontSize="13" Margin="0,0,0,4"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
            <TextBox x:Name="TbAmount" Height="36"
                     Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                     Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                     BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

            <TextBlock Text="备注" FontSize="13" Margin="0,12,0,4"
                       Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
            <TextBox x:Name="TbNote" Height="36"
                     Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"
                     Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                     BorderBrush="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="取消" Width="90" Height="38" Click="OnCancelClick"/>
            <Button x:Name="BtnConfirm" Content="确认转入" Width="90" Height="38" Margin="12,0,0,0" Click="OnConfirmClick"/>
        </StackPanel>
    </Grid>
</Window>
